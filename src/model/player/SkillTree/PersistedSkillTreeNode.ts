import { triggerNameType, statNameType } from "../../../types";

export interface PersistedSkillTreeNode {

    readonly rowIndex: number;
    readonly columnIndex: number; 
    readonly levelStudied: number 

}